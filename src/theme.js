
import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#8bbc1f',
    },
    secondary: {
      main: '#ffc000',
    },
    pale: {
     main: '#082D0E',
     contrastText: '#fff',
    }
  },
});